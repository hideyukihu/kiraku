/**
 *  @type {import('next').NextConfig} 
 * */
const nextConfig = {
  reactStrictMode: false,
}

module.exports = {
  reactStrictMode: false,
  // webpackDevMiddleware: config => {
  //   config.watchOptions = {
  //     poll: 5000,
  //     aggregateTimeout: 300,
  //   }
  //   return config
  // },
}
